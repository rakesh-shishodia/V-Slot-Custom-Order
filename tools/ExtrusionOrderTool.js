

import { renderToolLayout } from './OrderToolCore.js';

const extrusionConfig = {
  toolId: 'extrusion',
  containerId: 'main-ordering-tab'
};

// Render the extrusion tool UI layout
renderToolLayout(extrusionConfig);